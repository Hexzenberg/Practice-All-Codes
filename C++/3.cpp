/*
ID: Hexzenberg
PROG: gift1
LANG: C++ 
*/
#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define vll vector<ll>
#define endl '\n'
int32_t main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    ofstream fout("ride.out");
    ifstream fin("ride.in");
    ll n,s=0,m,p,k=0;fin>>n;string a[n];
    while(s<n)cin>>a[s++];
    string v[k];
    while(n--){
        cin>>v[k++];
        cin>>m>>p;
        while(p--){}
    }
    fout<<endl;
    return 0;
}